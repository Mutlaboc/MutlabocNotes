# Промпт: анимация идущего мужчины (славянский крестьянин) для Mutlaboc Notes

> Скопируй этот текст агенту. Задача рассчитана на наши умения:
> сначала **`mutlaboc-raster-asset-editor`** (подготовка пиксель-слоёв), затем
> **`mutlaboc-motion-lottie`** и его путь **B1 (Compose, `FilterQuality.None`)`**
> для покадровой пиксельной анимации. Делать через скрипты и гейты умений.

---

## 0. Цель

Создать пиксель-арт анимацию мужчины-крестьянина, который **ходит по переднему
плану перед домом** (`home.lottie` / `base_house.png`), **периодически
останавливается и задумывается, притоптывая ногой**, затем разворачивается и идёт
обратно. Анимация — **самодостаточная бесшовная петля** (ambient, как idle-петля
дома). Главное: точное соответствие стилю дома, чёткие пиксели, верные пропорции.

Техника: **покадровый спрайт** (классический pixel-art walk-cycle), отрисовка в
**Jetpack Compose** поверх PNG-кадров с `FilterQuality.None` (nearest-neighbour).
**НЕ** использовать Lottie/dotLottie для самого персонажа: пиксельный walk в Lottie
даёт либо тяжёлую растровую секвенцию, либо риг с вращением конечностей, который
размывает пиксели. Покадровый спрайт + Compose = максимально чётко и аутентично.

---

## 1. Стиль (обязательно соответствовать дому)

Эталон стиля — `base_house.png` (есть в `home.lottie`). Перед работой извлеки
палитру дома и залочь на неё все кадры персонажа:

- `extract_palette.py base_house.png --out house_palette.json --swatch house_palette.png`
- после отрисовки каждого кадра: `extract_palette.py <frame> --quantize --palette house_palette.json --out <locked>` (без дрейфа палитры).

Характеристики стиля дома, которые надо повторить:

- **Палитра**: тёплое дерево — тёмно-коричневые `(38,24,14)`, `(44,28,16)`,
  `(49,31,18)`; почти чёрный контур `(0,0,0)`; кремовые/охристые светлые тона
  ставен; акценты красный и зелёный (орнамент); янтарный тёплый свет. Для кожи/лица
  допускаются 2–3 приглушённых тёплых тона, но в общей земляной гамме дома.
- **Контур**: тёмный (почти чёрный) внешний контур фигуры, как у деревянных
  элементов дома.
- **Штриховка**: богатая пиксельная (не плоская заливка), 2–3 тона на материал, как
  на брёвнах/ставнях. Без градиентов, без сглаживания, без блюра.
- **Плотность пикселя**: дом нарисован с эффективным «арт-пикселем» примерно
  **5–6 px холста** на один пиксель арта. Персонаж ОБЯЗАН иметь ту же плотность
  пикселя (см. §2 — верификация наложением). Иначе он будет «не из того мира».

### Образ персонажа
Славянский крестьянин в тон дому: **косоворотка/рубаха** с пояском (можно с
красно-зелёной вышивкой-акцентом по вороту/подолу), **порты**, **сапоги**,
**борода и картуз/шапка**. Вид сбоку (профиль), 3/4 допустимо если так читается
лучше. Силуэт должен ясно читаться на размере экрана.

---

## 2. Пропорции и размещение (координаты дома, холст 1254×1254)

- Дом: холст 1254×1254, 30 fps, петля. Дом занимает y 38→1154 (земля ≈ **y 1154**),
  верх окон первого этажа ≈ y 846.
- **Рост мужчины: 300–340 px** (дефолт **320 px**, ≈ 0.28 высоты дома). Голова
  оказывается у верха окон первого этажа — выглядит соразмерно, не гигант и не
  карлик.
- **Линия земли (стопы): y ≈ 1150**, на той же земле, что и дом. Он на переднем
  плане → рисуется ПОВЕРХ слоя дома.
- **Зона ходьбы по X: примерно x 150 → 1100** (вдоль фасада, не выходя за дом).
- **Спрайт-канвас одного кадра**: компактный, напр. **180×340 px** (вокруг фигуры,
  прозрачный фон). Горизонтальное перемещение делает аниматор (Compose), а не
  перерисовка фигуры по холсту.

### Нативная сетка спрайта
Рисовать на маленькой нативной сетке и увеличивать nearest-neighbour, чтобы плотность
пикселя совпала с домом:

- нативный персонаж ≈ **32 шир × 60 выс арт-пикселей**;
- апскейл ×~5.5 (только NEAREST) → ≈ **176×330 px** на экране (в диапазоне роста);
- это даёт ~5–6 px холста на арт-пиксель — как у дома.

### Верификация масштаба (обязательный шаг)
Сложить кадр персонажа рядом с `base_house.png` на общем холсте (стопы на y≈1150) и
сравнить: (а) высоту головы относительно окон первого этажа; (б) размер пиксель-блока
персонажа против брёвен/ставен дома. Подогнать рост/апскейл, пока и пропорции, и
плотность пикселя не совпадут. Приложить это контрольное изображение к отчёту.

---

## 3. План кадров (30 fps, под темп дома)

Рисовать профиль, направление вправо; влево — зеркало (mirror), отдельные кадры не
нужны. Все кадры — на одинаковом канвасе 180×340, идеально выровнены (стопы на общей
базовой линии, без «подпрыгивания» опорной ноги).

- **Ходьба (walk), 8 кадров, петля**: contact → recoil/down → passing → high/up →
  contact (вторая нога) → … (стандартный 8-фазный цикл). Темп ≈ 2 шага/сек
  (≈ 12–15 кадров экрана на шаг при 30fps; играть кадры walk по кругу).
- **Разворот (turn), 2–3 кадра**: короткий поворот корпуса для смены направления.
- **Раздумье/стоп (think) — стойка + притоп**:
  - база: стойка чуть расслабленная, **рука к подбородку**, лёгкое «дыхание»
    (1–2 px колебание корпуса/головы);
  - **притоп**: суб-цикл из **4–6 кадров** — носок опорной ноги поднимается и
    пристукивает (раз в ~0.6–0.8 с, 2–3 притопа за стоп);
  - моргание/наклон головы — опционально, для живости.

Раскладка файлов (snake_case, прозрачный PNG, один канвас):
`man_walk_01.png … man_walk_08.png`, `man_turn_01.png … man_turn_03.png`,
`man_think_01.png … man_think_06.png` (где tap-фазы внутри think). Либо единый
спрайт-лист `man_sheet.png` + `man_frames.json` (карта кадров x,y,w,h,duration).

---

## 4. Растровый конвейер (умение `mutlaboc-raster-asset-editor`) — гейты

Для КАЖДОГО кадра, по порядку:

1. `inspect_png.py <frame> --pixel-art` — должен выйти с кодом 0 (нет color-leak;
   partial-alpha ≤ 0.02 — жёсткие края; число цветов в пределах палитры).
2. `defringe_pixel_art.py <frame> --out <clean>` — бинаризация альфы + обнуление
   скрытого цвета (никаких полупрозрачных краёв и RGB под прозрачными пикселями —
   это будущие «блёстки» при масштабировании на устройстве).
3. `extract_palette.py <clean> --quantize --palette house_palette.json --out <locked>`
   — без новых цветов (палитра дома).
4. `compare_png_bounds.py man_walk_01.png … man_think_06.png` — выход 0: все кадры
   одного размера и origin (иначе фигура будет дёргаться).

Контракт хендоффа в моушен-умение: пути всех кадров, размеры, отчёт по альфе
(leak=0), результат выравнивания, подтверждение залоченной палитры.

---

## 5. Анимация в Compose (умение `mutlaboc-motion-lottie`, путь B1)

Опирайся на `references/compose-pixel-effects.md`. Ключевые правила:

- Все кадры рисовать с **`FilterQuality.None`** (nearest-neighbour), класть в
  `app/src/main/res/drawable-nodpi/` (чтобы Android не пере-масштабировал по
  плотности и не сглаживал).
- **Покадровый драйвер**: продвигать индекс кадра по тикам (walk: ~один кадр каждые
  ~3–4 кадра экрана для нужного темпа; think/tap по своему таймингу). Хранить кадры
  как список painter’ов с `FilterQuality.None`.
- **Перемещение**: горизонтальный сдвиг персонажа — `animateFloat`/`Animatable`
  (translationX в координатах сцены), а не перерисовка спрайта. Скорость движения
  синхронизировать с темпом walk-кадров, чтобы стопы «не скользили» (no foot
  sliding): за один полный 8-кадровый цикл сдвиг = длина двух шагов.
- **Зеркало для левого направления**: `scaleX = -1` на спрайте (пиксели остаются
  чёткими).
- **Машина состояний петли** (самодостаточный ambient-цикл):
  `WalkRight (≈2.0с)` → `Settle (≈0.3с)` → `Think+Tap (≈2.5с, 2–3 притопа)` →
  `Turn (≈0.3с)` → `WalkLeft (≈2.0с)` → `Settle` → `Think+Tap` → `Turn` → повтор.
  Состояние и направление — в Compose/ViewModel-стейте; смысл (идёт/думает) не должен
  зависеть только от анимации.
- **Бесшовность**: конец полного цикла приводит персонажа в стартовую позицию и фазу
  (та же X, то же направление, тот же кадр) — без рывка.
- **Размещение**: тот же ground-line, что у дома (стопы y≈1150 в координатах сцены),
  слой персонажа ПОВЕРХ дома, под общим масштабированием сцены.
- **Доступность**: декоративный `Image` → `contentDescription = null`; уважать
  reduced-motion (при выключенной анимации — статичный кадр стойки); без частого
  мерцания.

---

## 6. Критерии приёмки (проверить и приложить пруфы)

- Плотность пикселя персонажа совпадает с домом (контрольное наложение из §2).
- Рост 300–340 px, голова у верха окон 1-го этажа; не гигант/не карлик.
- Стопы строго на базовой линии во всех кадрах (нет вертикального дрейфа опорной ноги).
- Ходьба читается естественно, без «скольжения» стоп (скорость = темпу кадров).
- Стоп-раздумье читается: рука у подбородка + 2–3 различимых притопа.
- Петля бесшовная (стартовая поза = конечная).
- Палитра залочена на дом (новых цветов нет); жёсткие края, leak=0.
- Чётко (без мыла) на реальном размере экрана в светлой и тёмной теме.
- Все растровые гейты §4 выходят с кодом 0; контракт хендоффа выполнен.

---

## 7. Параметры для подстройки (значения по умолчанию выше)

- `MAN_HEIGHT_PX` = 320 (диапазон 300–340)
- `GROUND_Y` = 1150
- `WALK_SPAN_X` = [150, 1100]
- `NATIVE_GRID` = 32×60 арт-пикселей, апскейл ×5.5 (NEAREST)
- `WALK_FRAMES` = 8, `TAP_FRAMES` = 4–6, `FPS` = 30
- темпы состояний: WalkRight/Left ≈2.0с, Think+Tap ≈2.5с, Settle/Turn ≈0.3с

## 8. Результаты (куда положить)

- Кадры/лист: `app/src/main/res/drawable-nodpi/man_*.png` (+ `man_frames.json` если
  единый лист).
- Compose-компонент анимации персонажа + интеграция в сцену перед домом.
- Контрольное наложение масштаба (персонаж рядом с домом) и короткое превью (GIF/MP4)
  для ревью.
- Отчёт: что сделано, результаты гейтов, как встроено, ограничения.
